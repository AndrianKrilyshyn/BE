spring.application.name=BKB

spring.datasource.url=${DB_URL:jdbc:mysql://localhost:3306/proctoring_db?createDatabaseIfNotExist=true&useSSL=false&allowPublicKeyRetrieval=true}
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}

spring.jpa.hibernate.ddl-auto=update
spring.jpa.open-in-view=false
spring.jpa.properties.hibernate.format_sql=true

app.jwt.secret=${JWT_SECRET:bWluaW11bS0yNTYtYml0LXNlY3JldC1rZXktZm9yLWhtYWMtc2hhLWNoYW5nZS1pbi1wcm9k}
app.jwt.expiration-ms=${JWT_EXPIRATION:86400000}

spring.rabbitmq.host=${RABBITMQ_HOST:localhost}
spring.rabbitmq.port=${RABBITMQ_PORT:5672}
spring.rabbitmq.username=${RABBITMQ_USER:guest}
spring.rabbitmq.password=${RABBITMQ_PASS:guest}

app.s3.bucket=${S3_BUCKET:proctoring-videos-proct}
app.s3.region=${S3_REGION:eu-north-1}
app.s3.presigned-url-ttl-minutes=${S3_URL_TTL:30}

